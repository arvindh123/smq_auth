[
    {grpcbox, [
        {client, #{
            channels => [
                {auth, [{http, "localhost", 7001, []}], #{}},
                {clients, [{http, "localhost", 7006, []}], #{}},
                {channels, [{http, "localhost", 7005, []}], #{}}
            ]
        }}
    ]}
].
